Board = require('./board.js');
Game = require('./game.js')

class Player {
  constructor(symbol) {
    this.symbol = symbol;
    // this.name = name
  };
}


let playerSymbol = new Player(' x ');
playerSymbol.symbol;


module.exports = Player;
